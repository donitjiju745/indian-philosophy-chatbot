# ============ CORE WEB FRAMEWORK ============
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12
starlette>=0.38.0

# ============ DATA VALIDATION ============
pydantic>=2.9.0
pydantic-settings>=2.5.0
typing-extensions>=4.12.0
annotated-types>=0.7.0

# ============ ENVIRONMENT & CONFIG ============
python-dotenv==1.0.0

# ============ HTTP & ASYNC ============
httpx>=0.27.0
aiohttp>=3.10.0
requests==2.31.0

# ============ DATABASE (SQLite) ============
sqlalchemy>=2.0.35
alembic>=1.14.0

# ============ FREE LLM SUPPORT (Optional) ============
# Uncomment when needed in later steps

# For local LLM inference (if using Ollama)
# ollama>=0.3.0

# For HuggingFace free inference API
# huggingface-hub>=0.25.0

# For LangChain orchestration (completely free tier available)
# langchain>=0.3.0
# langchain-community>=0.3.0

# ============ UTILITIES ============
python-json-logger>=2.0.7
colorama==0.4.6